name: FTP Auto Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Run Build Script
      run: python build.py

    - name: FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        # 👑 注意: レンタルサーバー上の「happy-for-all」フォルダへの正しいパスを指定してください
        # 例: /home/xxxx/www/for-welfare/happy-for-all/
        server-dir: /ここを実際のFTPパスに変更してください/
        local-dir: ./dist/
